Comandos GIT

PASTA REPOSITÓRIO GIT:
- git init
- git status (situação da pasta)
- git config user.name "Jéssica Medalha" (nome usuario)
- git config user.email "jessicamedalhnha@gmail.com"
- vim .gitignore (arquivos q eu não quero q o git se preocupe)
- git add nomedoarquivo
- git commit -m "adiciona o readme"
- git log (verifica usuario e modificação)


GITHUB
- criar repositorio github
-  git remote add origin https://github.com/jessicamedalha/Testando.git
- git remote -v (LISTA REPOSITORIOS)
- git push origin master (envio arquivo to github)
- git pull origin master (pucha arquivo)



